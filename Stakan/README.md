Реализован TCP client с подключением к серверу по заданному IP/port.
Добавлена отправка и прием сообщений по TCP-соединению, вывод ответов в консоль.
Обработаны базовые ошибки сети и сценарии разрыва соединения.

Стек технологий

- Язык: C++20
- Сборка: CMake 3.15+
- Boost: filesystem, system, serialization
- Тесты: Catch2
- Потоки: Threads

# Биржевой стакан

Проект представляет собой многопоточный сервис обработки торговых данных с реализацией упрощенного Order Book. Сервис принимает поток событий, обрабатывает их и рассылает клиентам обновления лучшего уровня.

## Микросервис Order Book

- принимает поток заявок
- формирует книгу заявок (bid/ask book)
- обрабатывает ордера (New, Cancel, Replace)
- рассчитывает лучшую цену (best bid / best ask)
- предоставляет подписку на обновления стакана по TCP
- публикует обновления в реальном времени

## Клиентское приложение

- поддерживает консольный интерфейс
- отображает текущий топ уровня стакана (best bid/ask)
- получает обновления по подписке

## Мой вклад

- Спроектировал публичный интерфейс клиентской библиотеки для взаимодействия пользовательских приложений с торговым сервисом.
- Выполнил рефакторинг модуля `bid_ask_book`: вынес логгер из логики стакана, сократил дублирование кода, доработал внутреннюю логику обработки заявок и потокобезопасность.
- Переписал и доработал часть функций стакана, включая `GetTopSnapshot`, под обновленную модель данных.
- Обновил существующие тесты под новую логику работы стакана и добавил дополнительные тесты для проверки новых сценариев.
- Внес изменения в клиентскую библиотеку и консольный клиент, чтобы пользователь мог получать и видеть `MDUpdate` с актуальными рыночными данными.
- Проект выполнялся по техническому заданию; реализована основная часть требований.

## Сборка и запуск

```bash
conan install . --output-folder=build --build=missing -s build_type=Release
cd build
cmake .. -DCMAKE_TOOLCHAIN_FILE=conan_toolchain.cmake -DCMAKE_BUILD_TYPE=Release
cmake --build . --config Release
```
